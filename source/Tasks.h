#pragma once

#include "BaseTask.h"

/**
 * \brief Manages the top side menu tab - "Tasks"
 */
class Tasks : public BaseTask {
public:
    // TODO: Set add task callback criteria (like filter in python)
    Tasks() : BaseTask() { }
};